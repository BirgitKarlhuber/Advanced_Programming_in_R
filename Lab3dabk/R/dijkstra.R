

dijkstra <- function(graph, init_node){
  stopifnot(is.data.frame(graph), length(graph)==3)
  
  for(vetex in graph){
    # dist[v] ← INFINITY
    # prev[v] ← UNDEFINED
    # add v to Q
    # dist[source] ← 0
  }
    
  
  
}
